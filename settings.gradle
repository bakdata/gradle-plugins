pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
        maven { url = "https://s01.oss.sonatype.org/content/repositories/snapshots" }
    }
}

rootProject.name = 'gradle-plugins'

include 'sonar', 'sonatype', 'release', 'jib', 'mockito'
